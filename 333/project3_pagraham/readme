Prerequisites:
* Node.JS >= 0.4 http://nodejs.org
* NPM http://npmjs.org/

Next, run the following to install required packages (this will install CoffeeScript):
$ npm install

To run the syntax analyzer, run the following ("filename" is the name of the tokens file outputted by flex):
$ node run.js filename